# Database Configuration
# Either use DATABASE_URL directly or use DATABASE_KEYFILE for key file approach
DATABASE_URL=postgres://username:password@localhost/finance_wise
# DATABASE_KEYFILE=./db_keyfile

# Database Pool Configuration
DB_POOL_SIZE=10
DB_TIMEOUT=30

# Server Configuration
SERVER_HOST=localhost
SERVER_PORT=8080

# Logging Configuration
LOG_LEVEL=info
# LOG_FILE=./logs/finwise.log
